import * as React from 'react';
import Container from '@mui/material/Container';
import BasicLineChart from '../src/LineChart';

export default function About() {
  return (
    <BasicLineChart />
  );
}
